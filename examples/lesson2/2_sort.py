# We start with a list of random 
unsorted_list = [5, 3, 1, 2, 4]

# We sort the list
sorted_list = sorted(unsorted_list)

# We print the sorted list
print(sorted_list)

# We start with a list of strings 
unsorted_list_2 = ['t', 'abc', 'z', 'w', 'b', 'a']

# We sort the list
sorted_list_2 = sorted(unsorted_list_2)

# We print the sorted list
print(sorted_list_2)
